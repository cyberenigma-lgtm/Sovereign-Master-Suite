[package]
name = "sovereign_master_suite"
version = "1.1.0"
edition = "2021"
authors = ["cyberenigma-lgtm"]
description = "Sovereign Master Suite — Ultra High Performance Rust Native Swiss Army Knife"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
